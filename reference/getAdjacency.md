# Adjacency matrix of a spatial weight structure

Adjacency matrix of a spatial weight structure

## Usage

``` r
getAdjacency(spec)

# S4 method for class 'SpatialWeightSpec'
getAdjacency(spec)
```

## Arguments

- spec:

  A
  [`SpatialWeightSpec`](https://madsyair.github.io/nimix/reference/SpatialWeightSpec-class.md).

## Value

The (named) numeric adjacency matrix.
