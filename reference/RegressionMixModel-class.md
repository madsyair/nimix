# Mixture-of-regressions model (for nimixReg)

Mixture-of-regressions model (for nimixReg)

## Slots

- `data`:

  Numeric response vector.

- `formula`:

  The model formula.

- `X`:

  The numeric design matrix (one row per observation, including the
  intercept column when present).
