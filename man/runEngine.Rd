% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-EngineConfig.R, R/engine-dpm.R,
%   R/engine-fixedk.R
\name{runEngine}
\alias{runEngine}
\alias{runEngine,DPMEngine-method}
\alias{runEngine,FixedKEngine-method}
\title{Run a mixture engine on a model (internal generic)}
\usage{
runEngine(
  engine,
  model,
  mcmcControl = list(),
  initMethod = "kmeans",
  seed = 1L,
  verbose = TRUE,
  ...
)

\S4method{runEngine}{DPMEngine}(
  engine,
  model,
  mcmcControl = list(),
  initMethod = "kmeans",
  seed = 1L,
  verbose = TRUE,
  ...
)

\S4method{runEngine}{FixedKEngine}(
  engine,
  model,
  mcmcControl = list(),
  initMethod = "kmeans",
  seed = 1L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{engine}{An \code{\linkS4class{EngineConfig}}.}

\item{model}{A \code{\linkS4class{MixtureModel}}.}

\item{mcmcControl, initMethod, seed, verbose}{Sampler controls.}

\item{...}{Reserved.}
}
\value{
A named list with the MCMC samples, the posterior of the number of
  occupied components, the allocation matrix, the parsed parameter traces,
  and the resolved MCMC control list.
}
\description{
Dispatches on the engine: \code{\linkS4class{DPMEngine}} builds a CRP model,
\code{\linkS4class{FixedKEngine}} builds a finite-mixture model. Returns the
raw pieces used to construct a \code{\linkS4class{FitResult}}.
}
\section{Functions}{
\itemize{
\item \code{runEngine(DPMEngine)}: Dirichlet Process Mixture run (NIMBLE dCRP).

\item \code{runEngine(FixedKEngine)}: Finite-mixture run with fixed K (Dirichlet weights +
categorical allocation).

}}
\keyword{internal}
