% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-SpatialWeightSpec.R
\name{spatialWeights}
\alias{spatialWeights}
\title{Construct a SpatialWeightSpec from an adjacency/weight matrix}
\usage{
spatialWeights(
  adjacency = NULL,
  regionIds = NULL,
  edges = NULL,
  nNodes = NULL,
  edgeWeights = NULL
)
}
\arguments{
\item{adjacency}{Numeric n x n matrix: symmetric, zero diagonal,
non-negative. Binary 0/1 contiguity is the typical case. Give either
this \emph{or} \code{edges}, not both.}

\item{regionIds}{Optional character vector of n unique region names.
Defaults to the matrix row names, or \code{"region1"}, ....}

\item{edges}{Alternative sparse input: a two-column matrix of node
indices, one row per undirected edge (order and duplicates are
normalised). Never allocates an \eqn{n \times n} matrix, so it is the
route for large graphs. Requires \code{nNodes}.}

\item{nNodes}{Number of nodes when constructing from \code{edges}.}

\item{edgeWeights}{Optional positive weights, one per row of
\code{edges}; defaults to 1 (binary contiguity).}
}
\value{
A validated \code{\linkS4class{SpatialWeightSpec}}.
}
\description{
Construct a SpatialWeightSpec from an adjacency/weight matrix
}
\examples{
A <- matrix(0, 3, 3); A[1, 2] <- A[2, 1] <- 1; A[2, 3] <- A[3, 2] <- 1
sw <- spatialWeights(A, regionIds = c("west", "centre", "east"))
nRegions(sw)
neighborsOf(sw, "centre")
}
