% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-DistributionSpec.R, R/dist-glm-reg.R,
%   R/dist-heavytail-reg.R
\name{responseRng}
\alias{responseRng}
\alias{responseRng,DistributionSpec-method}
\alias{responseRng,PoissonRegSpec-method}
\alias{responseRng,BinomialRegSpec-method}
\alias{responseRng,StudentTRegSpec-method}
\alias{responseRng,NormalGammaRegSpec-method}
\title{Draw a response given linear predictors and error scale}
\usage{
responseRng(spec, eta, s2 = NULL, prior = NULL, ...)

\S4method{responseRng}{DistributionSpec}(spec, eta, s2 = NULL, prior = NULL, ...)

\S4method{responseRng}{PoissonRegSpec}(spec, eta, s2 = NULL, prior = NULL, ...)

\S4method{responseRng}{BinomialRegSpec}(spec, eta, s2 = NULL, prior = NULL, ...)

\S4method{responseRng}{StudentTRegSpec}(spec, eta, s2 = NULL, prior = NULL, ...)

\S4method{responseRng}{NormalGammaRegSpec}(spec, eta, s2 = NULL, prior = NULL, ...)
}
\arguments{
\item{spec}{A distribution spec.}

\item{eta}{Numeric vector of linear predictors.}

\item{s2}{Numeric vector of error variances (Normal only; ignored
elsewhere).}

\item{prior}{Optional prior list (e.g. Binomial \code{size}).}

\item{...}{Unused.}
}
\value{
A numeric vector of draws, one per element of \code{eta}.
}
\description{
The family-specific tail of \code{\link{posteriorPredictive}}: given the
per-observation linear predictor(s) and (for a Normal) the error variance,
draw one response each. Defaults to the Gaussian
\eqn{y \sim N(\eta, \sigma^2)}; Poisson and Binomial override it with
their own link and sampler, so a predictive draw from a count model is a
count, not a Gaussian jitter around the mean.
}
\section{Methods (by class)}{
\itemize{
\item \code{responseRng(DistributionSpec)}: Gaussian: identity link, Normal noise.

\item \code{responseRng(PoissonRegSpec)}: Poisson: log link, Poisson draw.

\item \code{responseRng(BinomialRegSpec)}: Binomial: logit link, Binomial draw.

\item \code{responseRng(StudentTRegSpec)}: Student-t regression: identity link, scaled-t
noise. Both the direct Student-t and the Normal-Gamma augmentation have
the same Student-t marginal, so they share this. \code{s2} is the error
VARIANCE, which for a t with \code{df} degrees of freedom is
\eqn{\sigma^2 \, \mathrm{df}/(\mathrm{df}-2)}; the draw is
\eqn{\eta + \sigma \, t_{\mathrm{df}}} with
\eqn{\sigma = \sqrt{s2 (\mathrm{df}-2)/\mathrm{df}}}.

\item \code{responseRng(NormalGammaRegSpec)}: Normal-Gamma regression: same Student-t marginal
as the direct parameterisation, so the same scaled-t draw. Defined
explicitly because the two heavy-tail specs are siblings under
\code{NormalRegSpec}, not parent and child -- inheritance would give the
Gaussian default and silently drop the tails.

}}
